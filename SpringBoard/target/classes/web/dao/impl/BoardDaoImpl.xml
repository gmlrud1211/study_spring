<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="web.dao.face.BoardDao">
	<select id="selectAll" parameterType="web.util.Paging" resultType="web.dto.Board">
		select * from (
		 select rownum rnum, B.* from(
			select * from board
			order by board_no desc
		 ) B
		 order by rnum
		 ) R
		 where rnum between #{startNo} and #{endNo}
	</select>
	
	<select id="selectCntBoard" resultType="int">
		select count(*) from board	
	</select>
	
	<update id="updateHit" parameterType="web.dto.Board">
		update board
		set hit = #{hit}+1
		where board_no = #{board_no}
	</update>
	
	
	<select id="selectBoardByBoardno" parameterType="web.dto.Board" resultType="web.dto.Board">
		select * from board
		where board_no = #{board_no}
	</select>
</mapper>
